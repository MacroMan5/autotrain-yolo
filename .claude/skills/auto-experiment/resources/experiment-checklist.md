# Experiment Checklist

## Before

- [ ] Hypothesis written — "I expect X because Y, so I'll do Z"
- [ ] Action defined — strategic (`yolo-experiment run`) or HP optimization (`yolo-experiment tune`)

## After

- [ ] Journal entry appended to summary.md (schema from `resources/journal-schema.yaml`)
- [ ] Per-class AP checked against baseline
